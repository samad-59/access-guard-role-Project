{
  "name": "access-guard-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "vercel-build": "cd client && npm install && npm run build",
    "start": "cd client && npm start"
  },
  "workspaces": [
    "client",
    "server"
  ]
}
